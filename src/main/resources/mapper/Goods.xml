<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tardybird.goodsinfo.mapper.GoodsMapper">
    <resultMap id="goods" type="com.tardybird.goodsinfo.domain.Goods">
        <result property="id" column="id"/>
        <result property="gmtCreate" column="gmt_create"/>
        <result property="gmtModified" column="gmt_modified"/>
        <result property="name" column="name"/>
        <result property="goodsSn" column="good_sn"/>
        <result property="shortName" column="short_name"/>
        <result property="description" column="description"/>
        <result property="brief" column="brief"/>
        <result property="picUrl" column="pic_url"/>
        <result property="detail" column="detail"/>
        <result property="statusCode" column="status"/>
        <result property="shareUrl" column="share_url"/>
        <result property="gallery" column="gallery"/>
        <result property="goodsCategoryId" column="goods_category_ids"/>
        <result property="brandId" column="brand_id"/>
        <result property="beDeleted" column="is_deleted"/>
        <result property="weight" column="weight"/>
        <result property="volume" column="volume"/>
        <result property="specialFreightId" column="special_freight_id"/>
        <result property="beSpecial" column="is_special"/>
    </resultMap>

    <select id="getGoodsByCondition" parameterType="java.lang.String" resultMap="goods">
        SELECT *
        FROM goods
        WHERE (goods_sn = #{goodsSn} OR name = #{name})
          and is_deleted = 0
    </select>


    <select id="getAllGoods" resultMap="goods">
        select *
        from goods
    </select>

    <select id="findHotGoods" resultMap="goods">
        select *
        from goods
        where is_deleted = 0
          and status = 3
    </select>

    <select id="findNewGoods" resultMap="goods">
        select *
        from goods
        where status = 4
          and is_deleted = 0
    </select>

    <select id="getGoodsById" resultMap="goods">
        select *
        from goods
        where id = #{id}
          and is_deleted = 0
    </select>

    <insert id="createGoods" parameterType="com.tardybird.goodsinfo.domain.Goods" useGeneratedKeys="true"
            keyProperty="id">
        insert into goods(name, gmt_create, gmt_modified, goods_sn, short_name, description, detail, brief,
                          pic_url, status, share_url, gallery, goods_category_id, brand_id, is_deleted, weight, volume,
                          special_freight_id, is_special)
        values (#{name}, current_timestamp, current_timestamp, #{goodsSn}, #{shortName}, #{shareUrl}, #{description},
                #{detail}, #{brief}, #{picUrl}, #{statusCode}, #{gallery}, #{goodsCategoryId}, #{brandId},
                #{beDeleted}, #{weight}, #{volume}, #{specialFreightId}, #{beSpecial})

    </insert>

    <update id="updateGoods" parameterType="com.tardybird.goodsinfo.domain.Goods">
        update goods
        <set>
            <if test="name != null">
                name=#{name},
            </if>
            <if test="goodsSn != null">
                goods_sn=#{goodsSn},
            </if>
            <if test="shortName != null">
                short_name=#{shortName},
            </if>
            <if test="description != null">
                `description`=#{description},
            </if>
            <if test="brief != null">
                brief=#{brief},
            </if>
            <if test="picUrl != null">
                pic_url=#{picUrl},
            </if>
            <if test="detail != null">
                detail=#{detail},
            </if>
            <if test="statusCode != null">
                `status`=#{statusCode},
            </if>
            <if test="shareUrl != null">
                share_url=#{shareUrl},
            </if>
            <if test="gallery != null">
                gallery=#{gallery},
            </if>
            <if test="goodsCategoryId != null">
                goods_category_id=#{goodsCategoryId},
            </if>
            <if test="brandId != null">
                brand_id=#{brandId},
            </if>
            <if test="beDeleted != null">
                is_deleted=#{beDeleted},
            </if>
            <if test="weight != null">
                weight=#{weight},
            </if>
            <if test="volume != null">
                volume=#{volume},
            </if>
            <if test="specialFreightId != null">
                special_freight_id=#{specialFreightId},
            </if>
            <if test="beSpecial != null">
                is_special=#{beSpecial},
            </if>
            gmt_modified=now()
        </set>
        where id=#{id} and is_deleted=0
    </update>

    <update id="deleteGoods" parameterType="com.tardybird.goodsinfo.domain.Goods">
        update goods
        set is_deleted = 1
        where id = #{id}
    </update>

    <select id="selectOnSaleGoods" resultType="Integer">
        select count(status)
        from goods
        where status = 0
    </select>

</mapper>